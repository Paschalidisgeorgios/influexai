import { ContentEngine } from "@/components/admin/content-engine";

export const metadata = {
  title: "SEO Content Engine — Admin",
};

export default function AdminContentPage() {
  return <ContentEngine />;
}
