/** Legacy capsule toast — component was never mounted; callers keep a stable no-op API. */
export const capsuleShow = (_msg: string, _duration?: number) => {
  /* no-op: SmartCapsule UI removed in Tier 1 Wave A dead-code cleanup */
};
